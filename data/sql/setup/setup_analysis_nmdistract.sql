CREATE TABLE IF NOT EXISTS analysis.analysis_nmdistract (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            nmdistract INTEGER,
            nmdistract_name VARCHAR
        );