CREATE TABLE IF NOT EXISTS analysis.analysis_nmcrash (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            nmcc INTEGER,
            nmcc_name VARCHAR
        );