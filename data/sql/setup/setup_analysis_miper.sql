CREATE TABLE IF NOT EXISTS analysis.analysis_miper (
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            p1 INTEGER,
            p2 INTEGER,
            p3 INTEGER,
            p4 INTEGER,
            p5 INTEGER,
            p6 INTEGER,
            p7 INTEGER,
            p8 INTEGER,
            p9 INTEGER,
            p10 INTEGER
        );