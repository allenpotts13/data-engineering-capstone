CREATE TABLE IF NOT EXISTS analysis.analysis_safetyeq (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            nmhelmet INTEGER,
            nmhelmet_name VARCHAR,
            nmpropad INTEGER,
            nmpropad_name VARCHAR,
            nmothpro INTEGER,
            nmothpro_name VARCHAR,
            nmrefclo INTEGER,
            nmrefclo_name VARCHAR,
            nmlight INTEGER,
            nmlight_name VARCHAR,
            nmothpre INTEGER,
            nmothpre_name VARCHAR
        );