CREATE TABLE IF NOT EXISTS analysis.analysis_damage (
            state INTEGER,
            state_name TEXT,
            st_case INTEGER,
            veh_no INTEGER,
            damage INTEGER,
            damage_name TEXT
        );