CREATE TABLE IF NOT EXISTS analysis.analysis_crashrf (
            state INTEGER,
            state_name TEXT,
            st_case INTEGER,
            crashrf INTEGER,
            crashrf_name TEXT
        );