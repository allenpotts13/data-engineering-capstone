CREATE TABLE IF NOT EXISTS analysis.analysis_pbtype (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            pbage INTEGER,
            pbage_name VARCHAR,
            pbsex INTEGER,
            pbsex_name VARCHAR,
            pbptype INTEGER,
            pbptype_name VARCHAR,
            pbcwalk INTEGER,
            pbcwalk_name VARCHAR,
            pbswalk INTEGER,
            pbswalk_name VARCHAR,
            pbszone INTEGER,
            pbszone_name VARCHAR,
            pedctype INTEGER,
            pedctype_name VARCHAR,
            bikectype INTEGER,
            bikectype_name VARCHAR,
            pedloc INTEGER,
            pedloc_name VARCHAR,
            bikeloc INTEGER,
            bikeloc_name VARCHAR,
            pedpos INTEGER,
            pedpos_name VARCHAR,
            bikepos INTEGER,
            bikepos_name VARCHAR,
            peddir INTEGER,
            peddir_name VARCHAR,
            bikedir INTEGER,
            bikedir_name VARCHAR,
            motdir INTEGER,
            motdir_name VARCHAR,
            motman INTEGER,
            motman_name VARCHAR,
            pedleg INTEGER,
            pedleg_name VARCHAR,
            pedsnr INTEGER,
            pedsnr_name VARCHAR,
            pedcgp INTEGER,
            pedcgp_name VARCHAR,
            bikecgp INTEGER,
            bikecgp_name VARCHAR
        );