CREATE TABLE IF NOT EXISTS analysis.analysis_driverrf (
            state INTEGER,
            state_name TEXT,
            st_case INTEGER,
            veh_no INTEGER,
            driverrf INTEGER,
            driverrf_name TEXT
        );