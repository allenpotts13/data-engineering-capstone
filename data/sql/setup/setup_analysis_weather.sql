CREATE TABLE IF NOT EXISTS analysis.analysis_weather (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            weather INTEGER,
            weather_name VARCHAR
        );