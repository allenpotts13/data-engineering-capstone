CREATE TABLE IF NOT EXISTS analysis.analysis_maneuver (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            maneuver INTEGER,
            maneuver_name VARCHAR
        );