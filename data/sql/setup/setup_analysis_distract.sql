CREATE TABLE IF NOT EXISTS analysis.analysis_distract (
            state INTEGER,
            state_name TEXT,
            st_case INTEGER,
            veh_no INTEGER,
            drdistract INTEGER,
            drdistract_name TEXT
        );
