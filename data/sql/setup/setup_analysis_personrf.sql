CREATE TABLE IF NOT EXISTS analysis.analysis_personrf (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            personrf INTEGER,
            personrf_name VARCHAR
        );