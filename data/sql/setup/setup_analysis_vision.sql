CREATE TABLE IF NOT EXISTS analysis.analysis_vision (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            vision INTEGER,
            vision_name VARCHAR
        );