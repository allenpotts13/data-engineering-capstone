CREATE TABLE IF NOT EXISTS analysis.analysis_midrvacc (
            st_case INTEGER,
            a1 INTEGER,
            a2 INTEGER,
            a3 INTEGER,
            a4 INTEGER,
            a5 INTEGER,
            a6 INTEGER,
            a7 INTEGER,
            a8 INTEGER,
            a9 INTEGER,
            a10 INTEGER
        );