CREATE TABLE IF NOT EXISTS analysis.analysis_factor (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            vehiclecc INTEGER,
            vehiclecc_name VARCHAR
        );