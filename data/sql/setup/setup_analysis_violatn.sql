CREATE TABLE IF NOT EXISTS analysis.analysis_violatn (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            violation INTEGER,
            violation_name VARCHAR
        );