CREATE TABLE IF NOT EXISTS analysis.analysis_race (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            race INTEGER,
            race_name VARCHAR,
            "order" INTEGER,
            order_name VARCHAR,
            multrace INTEGER,
            multrace_name VARCHAR
        );