CREATE TABLE IF NOT EXISTS analysis.analysis_nmprior (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            nmaction INTEGER,
            nmaction_name VARCHAR
        );