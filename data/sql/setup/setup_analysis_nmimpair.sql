CREATE TABLE IF NOT EXISTS analysis.analysis_nmimpair (
            state INTEGER,
            state_name VARCHAR,
            st_case INTEGER,
            veh_no INTEGER,
            per_no INTEGER,
            nmimpair INTEGER,
            nmimpair_name VARCHAR
        );